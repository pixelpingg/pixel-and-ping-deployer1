# Optional: if the public site URL is known, set it here.
# Example on Replit: https://your-project.your-user.repl.co
PUBLIC_SITE_ORIGIN=
# Optional for a separately hosted frontend. Leave empty when the Node server serves the frontend itself.
VITE_API_BASE_URL=
